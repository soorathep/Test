.DS_Store

# make_people.py working folder — drop returned forms and photos here to
# process them; the results go into _data/people.yml and img/people/.
# The inbox itself is never committed.
inbox/
